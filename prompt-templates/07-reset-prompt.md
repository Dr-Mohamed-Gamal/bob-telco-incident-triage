# Reset the demo

**Mode:** Agent (start a **New Task**)

**Context:**

Run it after the meeting, to put the repository back to its starting state for the next demo. A fixed script does the reset, so no git commands are made up on the spot.

**Prompt:**

```
Reset the demo with bash scripts/reset_demo.sh.
```

**Expected result:**

`RESET: 0 changed files left`

---
